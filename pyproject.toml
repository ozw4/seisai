[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.workspace]
members = ["packages/*"]

[pytest]
markers = [
  "e2e: end-to-end tests (slow, needs env)",
]
