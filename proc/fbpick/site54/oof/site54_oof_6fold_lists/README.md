# Deprecated site54 OOF 6-fold list path

This directory is a legacy duplicate retained for existing configs and historical outputs.

Canonical fold lists now live at:

```text
/workspace/proc/fbpick/site54/oof/fold_lists
```

Do not add new configs or scripts that point at `/workspace/proc/fbpick/site54/oof/site54_oof_6fold_lists`. Use `/workspace/proc/fbpick/site54/oof/fold_lists` instead.

The canonical README is:

```text
/workspace/proc/fbpick/site54/oof/fold_lists/README.md
```
