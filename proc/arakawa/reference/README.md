# Arakawa Reference Files

Place user-provided reference grstat files here only when a repo-local path is
needed for a run.

Files in this directory are data artifacts and should not be committed. Keep
only this README tracked, and point configs at local files with
`paths.reference_grstat_path`.
