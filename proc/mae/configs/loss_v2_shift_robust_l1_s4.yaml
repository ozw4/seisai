base: base_mae_v2.yaml

paths:
  out_dir: ../runs/loss_v2_shift_robust_l1_s4

tracking:
  exp_name: loss_v2_shift_robust_l1_s4

train:
  losses:
    - kind: shift_robust_l1
      weight: 1.0
      scope: masked_only
      params:
        shift_max: 4
